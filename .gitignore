# Build artifacts
zig-out/
zig-cache/
*.o
*.a
*.so
*.dylib
*.dll

# Benchmark binaries
bin/

# Test artifacts
test_check
*.db
*.wal
*.sst
*.tmp

# Backstop: a literal `~` dir means some path used an unexpanded `~/.flo/data`
# instead of $HOME. The data dir is expanded at the runtime boundary now, but
# keep this so a stray one never gets committed. See stdx.fs.expandTilde.
/~/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
.github/copilot-instructions.md

.zig-cache/

*.txt

# Dashboard build artifacts
src/node/dashboard/dist/

# Dashboard generated files
src/node/dashboard/dist/
src/node/dashboard/assets.zig
.tools/
