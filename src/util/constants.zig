/// Constants for Flo.
/// Based on TigerBeetle's constants.
/// Sector size (4KB).
pub const sector_size = 4096;

/// Block size for grid storage (4KB).
pub const block_size = 4096;

/// Maximum message size (1MB).
pub const message_size_max = 1024 * 1024;

/// Tick duration in milliseconds.
pub const tick_ms = 10;
